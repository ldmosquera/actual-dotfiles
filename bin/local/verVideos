#!/bin/bash

export MPLAYER_VERBOSE=0

#mplayer -ss 1 -zoom -nostop-xscreensaver -vo gl2 -fs -nosound -loop 0 -msglevel "all=0" "$@"
mplayer -ss 1 -zoom -nostop-xscreensaver -fs -nosound -loop 0 -msglevel "all=0" "$@"

